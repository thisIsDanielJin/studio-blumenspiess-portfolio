import React, { FC } from "react";
import { PageTemplate } from "@/app/components/PageTemplate/PageTemplate";

/**
 * HomeCutoutPage is a draft page for the cutout image route.
 * @returns {React.ReactElement}
 */
const HomeCutoutPage: FC = (): React.ReactElement => (
    <PageTemplate>
        <h1>Home Cutout Page</h1>
    </PageTemplate>
);

export default HomeCutoutPage;
